import DriversManager from '@/lib/ui/screen-components/protected/vendor/drivers';

const DriversScreen = () => {
  return (
    <div className="screen-container">
      <DriversManager />
    </div>
  );
};

export default DriversScreen;
